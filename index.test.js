import { test, expect } from "vitest";

// This should probably go in a separate lib file:
test.todo("Glob finds all clover.xml files");
test.todo("Reads all xml files");
test.todo("Parses xml to json");
test.todo("Merges all clover json to single output of file data");
test.todo("Filters down based on files that were changed in PR");

// - Then do the same thing ^ on main
// - then create output comment
